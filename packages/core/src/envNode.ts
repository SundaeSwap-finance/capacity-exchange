/** For use in Node.js apps (reads from process.env). */
export function requireNodeEnv(name: string): string {
  const value = process.env[name];
  if (!value) {
    throw new Error(`Missing required environment variable: ${name}`);
  }
  return value;
}
