# DNS-over-HTTPS endpoint used by createDoHSrvResolver to resolve SRV records.
# Defaults to https://cloudflare-dns.com/dns-query when not set.
# Only relevant in browser environments or when using createDoHSrvResolver explicitly.
#DOH_URL=https://cloudflare-dns.com/dns-query
#DOH_URL=https://dns.google/resolve